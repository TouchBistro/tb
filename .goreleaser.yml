project_name: tb

before:
  hooks:
    - go mod download
    # you may remove this if you don't need go generate
    - go generate ./...
    - packr2 clean

builds:
  -
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X release.version={{.Version}} -X release.commit={{.ShortCommit}}
    goos:
      - darwin
    hooks:
      pre: packr2
    ignore:
      - goos: darwin
        goarch: 386

archives:
  -
    replacements:
      darwin: Darwin
      linux: Linux
      windows: Windows
      386: i386
      amd64: x86_64

checksum:
  name_template: 'checksums.txt'

snapshot:
  name_template: "{{ .Tag }}-next"

changelog:
  sort: asc
  filters:
    exclude:
    - '^docs:'
    - '^test:'

brews:
  -
    github:
      owner: TouchBistro
      name: homebrew-touchbistro
    download_strategy: GitHubPrivateRepositoryReleaseDownloadStrategy
    commit_author:
      name: ohsabry
      email: osabry@touchbistro.com

    test: |
      system "#{bin}/program --version"

    install: |
      bin.install "program"

release:
  github:
    owner: TouchBistro
    name: tb
  name_template: "{{.ProjectName}}-v{{.Version}}"
